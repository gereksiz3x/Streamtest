name: Streamtest Güncelleyici

on:
  schedule:
    - cron: '*/30 * * * *'  # Her 30 dakikada bir
  workflow_dispatch:  # Manuel çalıştırma

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 📥 Repoyu indir
        uses: actions/checkout@v3
      
      - name: 🐍 Python kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 📦 Requests kur
        run: pip install requests
      
      - name: 🚀 Betiği çalıştır
        run: |
          python streamtest_to_m3u.py
          echo "✅ Betik çalıştı"
      
      - name: 📂 Dosyaları listele
        run: |
          echo "📁 Ana dizin:"
          ls -la
          echo "📁 Outputs dizini:"
          ls -la outputs/ || echo "outputs/ dizini yok"
      
      - name: 💾 Değişiklikleri kaydet
        run: |
          git config --local user.name "GitHub Actions"
          git config --local user.email "actions@github.com"
          
          # outputs klasörünü ekle
          git add outputs/ || echo "outputs/ eklenemedi"
          
          # Değişiklik var mı?
          if git diff --staged --quiet; then
            echo "✅ Değişiklik yok"
          else
            git commit -m "🔄 Otomatik güncelleme $(date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ Değişiklikler push edildi"
          fi
